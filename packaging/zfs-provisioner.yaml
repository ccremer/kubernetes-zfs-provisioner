# Kube config path. If left empty, the provisioner will try to connect to the API if running within Kubernetes.
kube_config_path: ""
# If you have multiple provisioners (ZFS nodes), make them unique for each cluster
provisioner_instance: "gentics.com/zfs"
